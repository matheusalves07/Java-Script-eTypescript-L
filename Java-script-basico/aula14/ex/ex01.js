const nome = prompt ('Digite seu nome completo');

window.document.body.innerHTML = `Olá ${nome} <br> `;

// += incremente inserções

/*window.document.body.innerHTML += `O seu nome <strong>${nome}</strong><br>`;

window.document.body.innerHTML += `seu nome tem ${nome.length} letras <br>`;

window.document.body.innerHTML += `a seunda letra do seu nome é ${nome[1]} <br>`;

window.document.body.innerHTML += `qual o primeiro índice da letra ${} no seu nome ? <br>`;

window.document.body.innerHTML += `qual o últiimo indice d letra ${} no seu nome ? <br>`;

window.document.body.innerHTML += `as últimas 3 letras do seu nome são  <br>`;

window.document.body.innerHTML += `as palavras do seu nome são  <br>`;

window.document.body.innerHTML += `seu nome com letras maiúsculas  <br>`;

window.document.body.innerHTML += `seu nome com letras menúsculas <br>`;

